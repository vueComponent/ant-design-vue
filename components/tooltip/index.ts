import { withInstall } from '../_util/type';
import ToolTip from './Tooltip';

export type {
  TooltipProps,
  AdjustOverflow,
  PlacementsConfig,
  TooltipAlignConfig,
  PlacementTypes,
} from './Tooltip';

export default withInstall(ToolTip);
