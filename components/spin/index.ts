import Spin, { setDefaultIndicator } from './Spin';

export type { SpinProps } from './Spin';
export { spinProps } from './Spin';

/* istanbul ignore next */

export default Object.assign(Spin, {
  setDefaultIndicator,
});
